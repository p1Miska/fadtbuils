minecraft_version=1.21.11
# yarn_mappings is kept here for reference only - this project uses
# loom.officialMojangMappings() in build.gradle instead (see README),
# so this value isn't actually consumed anywhere.
yarn_mappings=1.21.11+build.6
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod Properties
mod_version=2.0
maven_group=com.kapiteon.fastbuild
archives_base_name=fastbuild

# Fabric API
fabric_api_version=0.141.6+1.21.11

org.gradle.jvmargs=-Xmx3G
org.gradle.parallel=true
